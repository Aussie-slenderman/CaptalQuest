import React from 'react';
import { View } from 'react-native';

// AI Advisor tab removed — this stub exists because expo-router requires a file for every route
export default function AdvisorScreen() {
  return <View />;
}
